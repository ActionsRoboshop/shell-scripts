name: THIS IS GIT-HUB Actions CD
run-name: "${{ github.actor }} is learning GitHub Actions"
on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: This is step-1
        run: echo "This is step ${names}"
        env:
          names: format('Hello {0} {1} {2}', 'chaitu', 'geetha', 'ram')  
            
              