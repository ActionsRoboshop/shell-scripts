name: THIS IS GIT-HUB Actions CD
run-name: "${{ github.actor }} is learning GitHub Actions"
on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: This is step-1
        run: echo "This is step 1"  

      - name: This is step-2 
        id: demo
        if: ${{ failure() }}
        run: echo "This is step-2"

      - name: This is step-2
        if: ${{ steps.demo.conclusion == 'failure' }}  
        run: echo "This is step 3"                 
            
              